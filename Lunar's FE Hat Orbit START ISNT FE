-- All Made By Myself. No Bull#hit --

-- Join My Discord Pls!: . --

print("Welcome Player To Lunar's FE Hat Orbit Script. Your Free From Keys. I Will Be Updating The Script Often And Not Often. Thanks For Using This!!! It Means Alot To Me :D. Some Scripts May Be Out Of Date. Sorry :()")

-- SERVICES --
local Players = game:GetService("Players")
local player = Players.LocalPlayer

-- GUI --
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Parent = player:WaitForChild("PlayerGui")
ScreenGui.ResetOnSpawn = false

-- Frames, Etc --

-- MAIN FRAME --
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 235, 0, 205)
MainFrame.Position = UDim2.new(0.5, -400, 0.5, -50)
MainFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
MainFrame.BackgroundTransparency = 0.5
MainFrame.BorderSizePixel = 0
MainFrame.Parent = ScreenGui

local UserInputService = game:GetService("UserInputService")

local dragging, dragInput, dragStart, startPos

local function update(input)
    local delta = input.Position - dragStart
    MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
end

MainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position

        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

MainFrame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        update(input)
    end
end)

Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 8)

-- SCROLLING FRAME (grey area) ---
local Scroll = Instance.new("ScrollingFrame") -- ✅ FIXED NAME
Scroll.Size = UDim2.new(0, 235, 0, 165)
Scroll.Position = UDim2.new(0, 0, 0, 40)
Scroll.CanvasSize = UDim2.new(0, 0, 0, 318)
Scroll.ScrollBarThickness = 6
Scroll.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Scroll.BackgroundTransparency = 0.7
Scroll.BorderSizePixel = 0
Scroll.Parent = MainFrame

Instance.new("UICorner", Scroll).CornerRadius = UDim.new(0, 8)

-- CLOSE BUTTON (top-right) --
local Close = Instance.new("TextButton")
Close.Size = UDim2.new(0, 22, 0, 22)
Close.AnchorPoint = Vector2.new(1, 0)
Close.Position = UDim2.new(1, 0, 0, 0)
Close.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Close.BackgroundTransparency = 0
Close.BorderSizePixel = 0
Close.Text = "X"
Close.TextScaled = true
Close.Font = Enum.Font.GothamBold
Close.TextColor3 = Color3.new(255, 255, 255)
Close.Parent = MainFrame

Instance.new("UICorner", Close).CornerRadius = UDim.new(0, 8)

Close.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

-- Text Label (middle-top) --
local Label = Instance.new("TextLabel")
Label.Size = UDim2.new(0, 160, 0, 32)
Label.AnchorPoint = Vector2.new(1, 0)
Label.Position = UDim2.new(1, -35, 0, 5)
Label.BackgroundColor3 = Color3.fromRGB(160, 160, 160)
Label.BackgroundTransparency = 1
Label.BorderSizePixel = 0
Label.Text = "Lunar's FE Insta-Death Hat Orbit"
Label.TextScaled = true
Label.TextSize = 10
Label.Font = Enum.Font.GothamBold
Label.TextColor3 = Color3.new(255, 255, 255)
Label.Parent = MainFrame

Instance.new("UICorner", Label).CornerRadius = UDim.new(0, 8)

-- Scroll Frame Buttons To Activate --
-- UI layout for scrolling buttons --

local Damping = 800

-- Create TextBox
local DampingBox = Instance.new("TextBox")
DampingBox.Size = UDim2.new(0, 105, 0, 38) -- fixed negative width
DampingBox.Position = UDim2.new(0, 0, 0, 280)
DampingBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
DampingBox.TextColor3 = Color3.fromRGB(255, 255, 255)
DampingBox.PlaceholderText = "Enter Damping..."
DampingBox.Text = tostring(Damping)
DampingBox.TextScaled = true
DampingBox.Font = Enum.Font.GothamBold
DampingBox.ClearTextOnFocus = false
DampingBox.Parent = Scroll
DampingBox.BackgroundTransparency = 0.5

Instance.new("UICorner", DampingBox).CornerRadius = UDim.new(0, 8)

-- Update variable when Enter is pressed
DampingBox.FocusLost:Connect(function(enterPressed)
	if enterPressed then
		local value = tonumber(DampingBox.Text)
		if value then
			Damping = value
			print("Damping changed to:", Damping)
		end
	end
end)

local Power = 50000

-- Create TextBox
local PowerBox = Instance.new("TextBox")
PowerBox.Size = UDim2.new(0, 105, 0, 38) -- fixed negative width
PowerBox.Position = UDim2.new(0, 0, 0, 240)
PowerBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
PowerBox.TextColor3 = Color3.fromRGB(255, 255, 255)
PowerBox.PlaceholderText = "Enter Power..."
PowerBox.Text = tostring(Power)
PowerBox.TextScaled = true
PowerBox.Font = Enum.Font.GothamBold
PowerBox.ClearTextOnFocus = false
PowerBox.Parent = Scroll
PowerBox.BackgroundTransparency = 0.5

Instance.new("UICorner", PowerBox).CornerRadius = UDim.new(0, 8)

-- Update variable when Enter is pressed
PowerBox.FocusLost:Connect(function(enterPressed)
	if enterPressed then
		local value = tonumber(PowerBox.Text)
		if value then
			Power = value
			print("Power changed to:", Power)
		end
	end
end)

local Height = 2

-- Create TextBox
local HeightBox = Instance.new("TextBox")
HeightBox.Size = UDim2.new(0, 105, 0, 38) -- fixed negative width
HeightBox.Position = UDim2.new(0, 0, 0, 200)
HeightBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
HeightBox.TextColor3 = Color3.fromRGB(255, 255, 255)
HeightBox.PlaceholderText = "Enter Height..."
HeightBox.Text = tostring(Height)
HeightBox.TextScaled = true
HeightBox.Font = Enum.Font.GothamBold
HeightBox.ClearTextOnFocus = false
HeightBox.Parent = Scroll
HeightBox.BackgroundTransparency = 0.5

Instance.new("UICorner", HeightBox).CornerRadius = UDim.new(0, 8)

-- Update variable when Enter is pressed
HeightBox.FocusLost:Connect(function(enterPressed)
	if enterPressed then
		local value = tonumber(HeightBox.Text)
		if value then
			Height = value
			print("Height changed to:", Height)
		end
	end
end)

local Speed = 1

-- Create TextBox
local SpeedBox = Instance.new("TextBox")
SpeedBox.Size = UDim2.new(0, 105, 0, 38) -- fixed negative width
SpeedBox.Position = UDim2.new(0, 0, 0, 160)
SpeedBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
SpeedBox.TextColor3 = Color3.fromRGB(255, 255, 255)
SpeedBox.PlaceholderText = "Enter Speed..."
SpeedBox.Text = tostring(Speed)
SpeedBox.TextScaled = true
SpeedBox.Font = Enum.Font.GothamBold
SpeedBox.ClearTextOnFocus = false
SpeedBox.Parent = Scroll
SpeedBox.BackgroundTransparency = 0.5

Instance.new("UICorner", SpeedBox).CornerRadius = UDim.new(0, 8)

-- Update variable when Enter is pressed
SpeedBox.FocusLost:Connect(function(enterPressed)
	if enterPressed then
		local value = tonumber(SpeedBox.Text)
		if value then
			Speed = value
			print("Speed changed to:", Speed)
		end
	end
end)

local Rotation = 0

-- Create TextBox
local RotateBox = Instance.new("TextBox")
RotateBox.Size = UDim2.new(0, 105, 0, 38) -- fixed negative width
RotateBox.Position = UDim2.new(0, 0, 0, 120)
RotateBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
RotateBox.TextColor3 = Color3.fromRGB(255, 255, 255)
RotateBox.PlaceholderText = "Enter Rotation..."
RotateBox.Text = tostring(Rotation)
RotateBox.TextScaled = true
RotateBox.Font = Enum.Font.GothamBold
RotateBox.ClearTextOnFocus = false
RotateBox.Parent = Scroll
RotateBox.BackgroundTransparency = 0.5

Instance.new("UICorner", RotateBox).CornerRadius = UDim.new(0, 8)

-- Update variable when Enter is pressed
RotateBox.FocusLost:Connect(function(enterPressed)
	if enterPressed then
		local value = tonumber(RotateBox.Text)
		if value then
			Rotation = value
			print("Rotate changed to:", Rotation)
		end
	end
end)

local Offset = 10

-- Create TextBox
local OffsetBox = Instance.new("TextBox")
OffsetBox.Size = UDim2.new(0, 105, 0, 38) -- fixed negative width
OffsetBox.Position = UDim2.new(0, 0, 0, 80)
OffsetBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
OffsetBox.TextColor3 = Color3.fromRGB(255, 255, 255)
OffsetBox.PlaceholderText = "Enter Offset..."
OffsetBox.Text = tostring(Offset)
OffsetBox.TextScaled = true
OffsetBox.Font = Enum.Font.GothamBold
OffsetBox.ClearTextOnFocus = false
OffsetBox.Parent = Scroll
OffsetBox.BackgroundTransparency = 0.5

Instance.new("UICorner", OffsetBox).CornerRadius = UDim.new(0, 8)

-- Update variable when Enter is pressed
OffsetBox.FocusLost:Connect(function(enterPressed)
	if enterPressed then
		local value = tonumber(OffsetBox.Text)
		if value then
			Offset = value
			print("Offset changed to:", Offset)
		end
	end
end)

-- Text Label (middle-top) --
local Label1 = Instance.new("TextLabel")
Label1.Size = UDim2.new(0, 60, 0, 38)
Label1.AnchorPoint = Vector2.new(1, 0)
Label1.Position = UDim2.new(1, -5, 0, 80)
Label1.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Label1.BackgroundTransparency = 0.5
Label1.BorderSizePixel = 0
Label1.Text = "Offset"
Label1.TextScaled = true
Label1.TextSize = 10
Label1.Font = Enum.Font.GothamBold
Label1.TextColor3 = Color3.new(255, 255, 255)
Label1.Parent = Scroll

Instance.new("UICorner", Label1).CornerRadius = UDim.new(0, 8)

-- Text Label (middle-top) --
local Label1 = Instance.new("TextLabel")
Label1.Size = UDim2.new(0, 60, 0, 38)
Label1.AnchorPoint = Vector2.new(1, 0)
Label1.Position = UDim2.new(1, -5, 0, 120)
Label1.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Label1.BackgroundTransparency = 0.5
Label1.BorderSizePixel = 0
Label1.Text = "Rotation"
Label1.TextScaled = true
Label1.TextSize = 10
Label1.Font = Enum.Font.GothamBold
Label1.TextColor3 = Color3.new(255, 255, 255)
Label1.Parent = Scroll

Instance.new("UICorner", Label1).CornerRadius = UDim.new(0, 8)

-- Text Label (middle-top) --
local Label1 = Instance.new("TextLabel")
Label1.Size = UDim2.new(0, 60, 0, 38)
Label1.AnchorPoint = Vector2.new(1, 0)
Label1.Position = UDim2.new(1, -5, 0, 160)
Label1.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Label1.BackgroundTransparency = 0.5
Label1.BorderSizePixel = 0
Label1.Text = "Speed"
Label1.TextScaled = true
Label1.TextSize = 10
Label1.Font = Enum.Font.GothamBold
Label1.TextColor3 = Color3.new(255, 255, 255)
Label1.Parent = Scroll

Instance.new("UICorner", Label1).CornerRadius = UDim.new(0, 8)

-- Text Label (middle-top) --
local Label1 = Instance.new("TextLabel")
Label1.Size = UDim2.new(0, 60, 0, 38)
Label1.AnchorPoint = Vector2.new(1, 0)
Label1.Position = UDim2.new(1, -5, 0, 200)
Label1.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Label1.BackgroundTransparency = 0.5
Label1.BorderSizePixel = 0
Label1.Text = "Height"
Label1.TextScaled = true
Label1.TextSize = 10
Label1.Font = Enum.Font.GothamBold
Label1.TextColor3 = Color3.new(255, 255, 255)
Label1.Parent = Scroll

Instance.new("UICorner", Label1).CornerRadius = UDim.new(0, 8)

-- Text Label (middle-top) --
local Label1 = Instance.new("TextLabel")
Label1.Size = UDim2.new(0, 60, 0, 38)
Label1.AnchorPoint = Vector2.new(1, 0)
Label1.Position = UDim2.new(1, -5, 0, 240)
Label1.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Label1.BackgroundTransparency = 0.5
Label1.BorderSizePixel = 0
Label1.Text = "Power"
Label1.TextScaled = true
Label1.TextSize = 10
Label1.Font = Enum.Font.GothamBold
Label1.TextColor3 = Color3.new(255, 255, 255)
Label1.Parent = Scroll

Instance.new("UICorner", Label1).CornerRadius = UDim.new(0, 8)

-- Text Label (middle-top) --
local Label1 = Instance.new("TextLabel")
Label1.Size = UDim2.new(0, 60, 0, 38)
Label1.AnchorPoint = Vector2.new(1, 0)
Label1.Position = UDim2.new(1, -5, 0, 280)
Label1.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Label1.BackgroundTransparency = 0.5
Label1.BorderSizePixel = 0
Label1.Text = "Damping"
Label1.TextScaled = true
Label1.TextSize = 10
Label1.Font = Enum.Font.GothamBold
Label1.TextColor3 = Color3.new(255, 255, 255)
Label1.Parent = Scroll

Instance.new("UICorner", Label1).CornerRadius = UDim.new(0, 8)

-- Auto resize canvas --

-- Create a button inside the scroll frame --




local Button1 = Instance.new("TextButton")
Button1.Size = UDim2.new(1, -5, 0, 38)
Button1.BackgroundColor3 = Color3.fromRGB(40, 0, 60)
Button1.Position = UDim2.new(0, 0, 0, 0)
Button1.Text = "Activate Hat Orbit"
Button1.TextColor3 = Color3.new(1, 1, 1)
Button1.TextScaled = true
Button1.TextSize = 10
Button1.Font = Enum.Font.GothamBold
Button1.BorderSizePixel = 0
Button1.Parent = Scroll
Button1.BackgroundTransparency = 0.3

Instance.new("UICorner", Button1).CornerRadius = UDim.new(0, 6)

Button1.MouseButton1Click:Connect(function()
    local LC = game.Players.LocalPlayer
local Name = LC.Name
local Char = LC.Character
local Humanoid = Char:FindFirstChildWhichIsA("Humanoid")
local Root = Humanoid.RootPart

local Accessories = Humanoid:GetAccessories()

local Target = Char
local TargetPos = Target.Humanoid.RootPart.Position









local Mode = 1

local Following = true
local Spin = true
local Rotating = true

local AkaliNotif = loadstring(game:HttpGet("https://raw.githubusercontent.com/Kinlei/Dynissimo/main/Scripts/AkaliNotif.lua"))();
local Notify = AkaliNotif.Notify;

Notify({
	Description = "This Script Was Made By: Lunar (fatbirdsdontfly_lol)";
	Title = "Lunar's FE Insta-Death Hat Orbit";
	Duration = 1.5;
});

Notify({
	Description = "This Is Currently In Beta. Only Deaths Activate The Hat Orbit. Works Both R6 And R15.";
	Title = "Lunar's FE Insta-Death Hat Orbit";
	Duration = 2;
});

Notify({
	Description = "Thanks For Using This Script!";
	Title = "Lunar's FE Insta-Death Hat Orbit";
	Duration = 2.5;
});

Notify({
	Description = "Start Hat Orbit Is Client Sided. When Died, Its FE For A Few Seconds :(";
	Title = "Lunar's FE Insta-Death Hat Orbit";
	Duration = 6;
});

function findName(pname)
	for i, v in ipairs(game.Players:GetPlayers()) do
		if pname then
			if string.match(v.Name:lower(), pname:lower()) or string.match(v.Character.Humanoid.DisplayName:lower(), pname:lower()) then
				return v.Name
			end
		else
		end
	end
end

function findChar(pname)
	return game.Players:FindFirstChild(findName(pname)).Character
end

function Netless()
	for i,v in next, game:GetService("Players").LocalPlayer.Character:GetDescendants() do
		if v:IsA("BasePart") and v.Name ~="HumanoidRootPart" then 
			game:GetService("RunService").Heartbeat:connect(function()
				v.Velocity = Vector3.new(-30,0,0)
			end)
		end
	end
end

Netless()

local hats = {}

if Target then
	local character = Target
	for _, hat in ipairs(character:GetChildren()) do
		if hat:IsA("Accessory") then
			hats[#hats+1] = hat
		end
	end
end

local hatCount = #hats
if hatCount > 0 then
	local angle = math.pi * 2 / hatCount
	for i, hat in ipairs(hats) do
		local handle = hat.Handle
		handle.AccessoryWeld:Remove()

		if handle then
			local bodyPosition = Instance.new("BodyPosition", handle)
			bodyPosition.MaxForce = Vector3.new(500000, 500000, 500000)
			bodyPosition.P = Power
			bodyPosition.D = Damping

			local bodyGyro = Instance.new("BodyGyro", handle)
			bodyGyro.MaxTorque = Vector3.new(500000, 500000, 500000)
			bodyGyro.P = Power
			bodyGyro.D = Damping

			local x = math.sin(Rotation + angle * (i-1))*Offset
			local z = math.cos(Rotation + angle * (i-1))*Offset

			bodyPosition.Position = TargetPos + Vector3.new(x, Height, z)
		end
	end

	local function myCoroutine()
		while wait(-9e999) do
			Rotation = Rotation + (Speed / 20)
			if Rotation >= math.pi * 2 then
				Rotation = 0
			end

			for i, hat in ipairs(hats) do
				local handle = hat.Handle
				local x = math.sin(Rotation + angle * (i-1))*Offset
				local z = math.cos(Rotation + angle * (i-1))*Offset

				handle.BodyPosition.P = Power

				handle.Velocity = Vector3.new(0, 0, 0)
				handle.Massless = true
				handle.CustomPhysicalProperties = PhysicalProperties.new(0, 0, 0, 0, 0)

				if Rotating == true then
					if Accessories[i+1] then
						handle.BodyGyro.CFrame = CFrame.lookAt(handle.Position, Accessories[i+1].Handle.Position)
					elseif not Accessories[i+1] then
						handle.BodyGyro.CFrame = CFrame.lookAt(handle.Position, Accessories[i-1].Handle.Position)
					else
					end
				end

				if Spin == true then
					handle.BodyPosition.Position = TargetPos + Vector3.new(x + Target.Humanoid.MoveDirection.X, Height, z + Target.Humanoid.MoveDirection.Z)
				end

				if Following == true then
					if Target == Char then
						TargetPos = Target.Humanoid.RootPart.Position+(Target.Humanoid.MoveDirection+Target.Humanoid.RootPart.Velocity)/150

					else
						TargetPos = Target.Humanoid.RootPart.Position+(Target.Humanoid.MoveDirection+Target.Humanoid.RootPart.Velocity)/5
					end
				end
			end
		end
	end

	local myWrappedCoroutine = coroutine.wrap(myCoroutine)

	myWrappedCoroutine()
end

function Mode2()
	if Mode == 2 then
		local Angle = math.pi * 2 / #hats

		function Loop()
			if Mode == 2 then
				local RootOrientation = Target.Humanoid.RootPart.CFrame - Target.Humanoid.RootPart.Position
				local RootRotation = RootOrientation

				for i, Hat in ipairs(hats) do
					local HatRotation = RootRotation.Y + Angle * (i - 1) + Speed * tick()
					local x = math.sin(HatRotation)*Offset
					local z = math.cos(HatRotation)*Offset

					local HatPos = TargetPos + RootOrientation * Vector3.new(x, z, -Height)
					Hat:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = HatPos
				end

				wait()
				Loop()
			end
		end

		Loop()

		for i, Hat in ipairs(hats) do
			local Handle = Hat.Handle

			Hat:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos
		end
	end
end


function Mode3()
	if Mode == 3 then
		for i = 1, #Accessories do
			Accessories[i]:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + Vector3.new(0, Height, 0)
			wait(.15)
		end
		wait(.15)
		Mode3()
	end
end

function Mode4 ()
	if Mode == 4 then
		if not LC:GetMouse().Target then else
			TargetPos = LC:GetMouse().Hit.Position
		end
		wait(-9e999)
		Mode4()
	end
end

function Mode5 ()
	local spiralPitch = 0
	local spiralAngle = 0

	function Loop ()
		if Mode == 5 then
			spiralAngle = spiralAngle + Speed / 300
			if spiralAngle >= math.pi * 10 then
				spiralAngle = 0
			end

			for i, hat in ipairs(hats) do
				local handle = hat.Handle
				if handle then
					local x = math.sin(spiralAngle + i * spiralPitch) * (i*Offset / 8)
					local y = i * (Height / 3)
					local z = math.cos(spiralAngle + i * spiralPitch) * (i*Offset / 8)
					handle.BodyPosition.Position = TargetPos - Vector3.new(0, 2, 0) + Vector3.new(x, y, z)
				end
			end
		end
		spiralPitch += Speed / 70
		wait(-9e999)
		Loop()
	end

	Loop()
end

function Mode6 ()
	local stack1 = {}
	local stack2 = {}

	for i = 1, #Accessories do
		if i <= #Accessories / 2 then
			stack1[#stack1 + 1] = Accessories[i]
		else
			stack2[#stack2 + 1] = Accessories[i]
		end
	end

	function Loop()
		if Mode == 6 then
			local angle = tick() * Speed
			local x = math.sin(angle)*Offset
			local z = math.cos(angle)*Offset

			for i, v in ipairs(stack1) do
				v:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + Vector3.new(x, i+Height,-z)
			end

			for i, v in ipairs(stack2) do
				v:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + Vector3.new(-x, i+Height,z)
			end
			wait()
			Loop()
		end
	end

	Loop()
end

function Mode7()
	local stack1 = {}
	local stack2 = {}
	local stack3 = {}

	for i = 1, #Accessories do
		if i < #Accessories / 3 then
			stack1[#stack1 + 1] = Accessories[i]
		elseif i < #Accessories / 3 * 2 or i == #Accessories then
			stack2[#stack2 + 1] = Accessories[i]
		else
			stack3[#stack3 + 1] = Accessories[i]
		end
	end


	function Loop()
		if Mode == 7 then
			local angle = tick() * Speed
			local x = math.sin(angle)*Offset
			local z = math.cos(angle)*Offset

			for i, v in ipairs(stack1) do
				v:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + Vector3.new(x, i+Height, -z)
			end

			for i, v in ipairs(stack2) do
				v:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + Vector3.new(x, i+Height, z)
			end

			for i, v in ipairs(stack3) do
				v:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + Vector3.new(-x, i+Height, -z)
			end
			wait()
			Loop()
		end
	end

	Loop()
end

function Mode8()
	if Mode == 8 then
		local forward = workspace.CurrentCamera.CFrame.LookVector
		local right = workspace.CurrentCamera.CFrame.RightVector
		local up = workspace.CurrentCamera.CFrame.UpVector
		local angle = math.pi * 2 / #hats * tick()

		for i, hat in ipairs(hats) do
			local handle = hat.Handle
			local x = right * (math.sin(angle * (i-1))*Offset)
			local y = up * (math.cos(angle * (i-1))*Offset)
			local z = forward * (Height+10)
			local pos = workspace.CurrentCamera.CFrame.LookVector + z + x + y
			local look = (workspace.CurrentCamera.CFrame.LookVector - pos).unit

			handle.BodyPosition.Position = pos + TargetPos + Vector3.new(0, 2, 0)
		end

		wait()
		Mode8()
	end
end

function Mode9 ()
	local Circle1 = {}
	local Circle2 = {}
	for i, v in pairs(Accessories) do
		if (#Circle1 < #Accessories / 2) then
			Circle1[#Circle1 + 1] = v
		else
			Circle2[#Circle2 + 1] = v
		end
	end

	function Loop ()
		if Mode == 9 then
			for i = 1, #Circle1 do
				local angle = tick() * Speed
				local x = math.sin(angle + (i * 5))*Offset
				local z = math.cos(angle + (i * 5))*Offset

				Circle1[i]:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + Vector3.new(x, Height, z)
			end

			for i = 1, #Circle2 do
				local angle = tick() * Speed
				local x = math.sin(angle + (i * 5))*Offset
				local z = math.cos(angle + (i * 5))*Offset

				Circle2[i]:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + Vector3.new(z, Height, x)
			end
			wait()
			Loop()
		end
	end

	Loop()
end

function Mode10 ()
	local Left = {}
	local Right = {}

	for i, v in pairs(Accessories) do
		if (#Left < #Accessories / 2) then
			Left[#Left + 1] = v
		else
			Right[#Right + 1] = v
		end
	end


	function Loop ()
		if Mode == 10 then
			for i, v in ipairs(Left) do
				local angle = tick() * Speed
				local x = math.sin(angle + i)*Offset
				local z = math.cos(angle + i)*Offset

				v:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + Vector3.new(z, x + Height, -x)
			end

			for i, v in ipairs(Right) do
				local angle = tick() * Speed
				local x = math.sin(angle + i)*Offset
				local z = math.cos(angle + i)*Offset

				v:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + Vector3.new(-x, x + Height, -z)
			end
			wait()
			Loop()
		end
	end

	Loop()
end

function Mode11 ()
	local OldOffset = Offset

	local Circle1 = {}
	local Circle2 = {}
	for i, v in pairs(Accessories) do
		if (#Circle1 < #Accessories / 2) then
			Circle1[#Circle1 + 1] = v
		else
			Circle2[#Circle2 + 1] = v
		end
	end

	function Loop ()
		if Mode == 11 then
			for i = 1, #Circle1 do
				local angle = tick() * Speed
				local x = -math.sin(angle + (i * angle))*Offset
				local y = math.cos(angle) / 2 * OldOffset
				local z = math.cos(angle + (i * -angle))*Offset

				Offset = math.sin(angle) / 2 * OldOffset

				local offset = CFrame.Angles(0,math.rad( Target.Humanoid.RootPart.Orientation.Y), 0) * Vector3.new(x, Height+y, z)
				Circle1[i]:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + offset
			end

			for i = 1, #Circle2 do
				local angle = tick() * Speed
				local x = -math.sin(angle + (i * angle))*Offset
				local y = -math.cos(angle) / 2 * OldOffset
				local z = math.cos(angle + (i * angle))*Offset

				Offset = math.sin(angle) / 2 * OldOffset

				local offset = CFrame.Angles(0, math.rad(Target.Humanoid.RootPart.Orientation.Y), 0) * Vector3.new(x, Height+y, z)
				Circle2[i]:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + offset
			end
			wait()
			Loop()
		end
	end

	Loop()
end

function Mode12 ()
	local Circle1 = {}
	local Circle2 = {}
	for i, v in pairs(Accessories) do
		if (#Circle1 < #Accessories / 2) then
			Circle1[#Circle1 + 1] = v
		else
			Circle2[#Circle2 + 1] = v
		end
	end

	function Loop ()
		if Mode == 12 then
			for i = 1, #Circle1 do
				local angle = tick() * Speed
				local x = math.sin(angle + (i * 5))*Offset
				local z = math.cos(angle + (i * 5))*Offset
				local offset = CFrame.Angles(0, math.rad(Target.Humanoid.RootPart.Orientation.Y), 0) * Vector3.new(x, Height, z)
				Circle1[i]:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + offset
			end

			for i = 1, #Circle2 do
				local angle = tick() * Speed
				local x = math.sin(angle + (i * 5))*Offset
				local z = math.cos(angle + (i * 5))*Offset
				local offset = CFrame.Angles(0, math.rad(-Target.Humanoid.RootPart.Orientation.Y), 0) * Vector3.new(x, Height + 2, z)
				Circle2[i]:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + offset
			end
			wait()
			Loop()
		end
	end

	Loop()
end

function Mode13 ()
	local Circle1 = {}
	local Circle2 = {}
	for i, v in pairs(Accessories) do
		if (#Circle1 < #Accessories / 2) then
			Circle1[#Circle1 + 1] = v
		else
			Circle2[#Circle2 + 1] = v
		end
	end

	function Loop ()
		if Mode == 13 then
			for i = 1, #Circle1 do
				local angle = tick() * Speed
				local x = math.sin(angle + (i * 5))*Offset
				local z = math.cos(angle + (i * 5))*Offset
				local offset = CFrame.Angles(0, math.rad(Target.Humanoid.RootPart.Orientation.Y), 0) * Vector3.new(x + Offset * 2, Height, z)
				Circle1[i]:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + offset
			end

			for i = 1, #Circle2 do
				local angle = tick() * Speed
				local x = math.sin(angle + (i * 5))*Offset
				local z = math.cos(angle + (i * 5))*Offset
				local offset = CFrame.Angles(0, math.rad(Target.Humanoid.RootPart.Orientation.Y), 0) * Vector3.new(x - Offset * 2, Height, z)
				Circle2[i]:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + offset
			end
			wait()
			Loop()
		end
	end

	Loop()
end

function Mode14()
	if Mode == 14 then
		for i, v in pairs(Accessories) do
			local Angle = math.sin(tick())*Speed

			local x = math.sin(Angle * (i-1)) * Offset
			local y = math.sin(Angle * (i-1)) + ((i*Height/5)+Angle)
			local z = math.cos(Angle * (i-1)) * Offset
			
			v:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + Vector3.new(x, y-2, z)
		end
		wait()
		Mode14()
	end
end

function Mode15()
	Height = -1

	function Loop ()
		if Mode == 15 then
			for i = 1, #Accessories do
				local offset = CFrame.Angles(0, math.rad(Target.Humanoid.RootPart.Orientation.Y), 0) * Vector3.new(0, Height, -i*Offset / 5)
				Accessories[i]:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + offset
			end

			wait()
			Loop()
		end
	end

	Loop()
end

function Mode16()
	local function Loop()
		if Mode == 16 then
			for i, v in pairs(Accessories) do
				local x = math.cos(math.random(1, 255) + (i + 1))*Offset
				local z = math.sin(math.random(1, 255) + (i + 1))*Offset

				local m = math.random(1, 13)
				if m == 1 then
					v:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + Vector3.new(x, Height, z)
				elseif m == 2 then
					v:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + Vector3.new(z, Height, x)
				elseif m == 3 then
					v:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + Vector3.new(-x, Height, z)
				elseif m == 4 then
					v:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + Vector3.new(x, Height, -z)
				elseif m == 5 then
					v:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + Vector3.new(x, z, z)
				elseif m == 6 then
					v:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + Vector3.new(x, x, z)
				elseif m == 7 then
					v:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + Vector3.new(-x, x, z)
				elseif m == 8 then
					v:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + Vector3.new(x, -x, z)
				elseif m == 9 then
					v:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + Vector3.new(x, x, -z)
				elseif m == 10 then
					v:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + Vector3.new(-x, z, z)	
				elseif m == 11 then
					v:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + Vector3.new(x, -z, z)	
				elseif m == 12 then
					v:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + Vector3.new(x, z, -z)	
				elseif m == 13 then
					v:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + Vector3.new(z, z, z)		
				end
			end
		end
		wait()
		Loop()
	end

	Loop()
end

function Mode17()
	local Circle1 = {}
	local Circle2 = {}

	local Offset2 = 0
	local Height2 = 0

	for i, v in pairs(Accessories) do
		if (#Circle1 < #Accessories / 2) then
			Circle1[#Circle1 + 1] = v
		else
			Circle2[#Circle2 + 1] = v
		end
	end

	function Loop ()
		if Mode == 17 then
			local angle = tick() * Speed+1
			for i = 1, #Circle1 do
				local x = math.cos(angle + (i * #hats))*Offset2/2
				local z = math.sin(angle + (i * #hats))*Offset2/2

				Circle1[i]:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + Vector3.new(x, -Height2, z)
			end

			for i = 1, #Circle2 do
				local x = math.cos(angle + (i * #hats))*Offset2/2
				local z = math.sin(angle + (i * #hats))*Offset2/2
				Circle2[i]:FindFirstChild("Handle"):FindFirstChild("BodyPosition").Position = TargetPos + Vector3.new(x, Height2, z)
			end

			Offset2 = math.sin(angle) * Offset
			Height2 = math.cos(angle) * Height
			wait()
			Loop()
		end
	end

	Loop()
end

local connect = LC.Chatted:Connect(function(chat)
	local Split = chat:lower():split(" ")

	local C1 = Split[1]
	local C2 = Split[2]

	if C1 == ".mode" then
		Mode = tonumber(C2)
		if C2 == "1" then
			Following = true
			Spin = true
			Rotating = true
		elseif C2 == "2" then
			Following = true
			Spin = false
			Rotating = true
			Mode2()		
		elseif C2 == "3" then
			Following = true
			Spin = false
			Rotating = true
			Mode3()
		elseif C2 == "4" then
			Following = false
			Spin = true
			Rotating = true
			Mode4()
		elseif C2 == "5" then
			Following = true
			Spin = false
			Rotating = true
			Mode5()
		elseif C2 == "6" then
			Following = true
			Spin = false
			Rotating = true
			Mode6()
		elseif C2 == "7" then
			Following = true
			Spin = false
			Rotating = true
			Mode7()
		elseif C2 == "8" then
			Following = true
			Spin = false
			Rotating = true
			Mode8()
		elseif C2 == "9" then
			Following = true
			Spin = false
			Rotating = true
			Mode9()
		elseif C2 == "10" then
			Following = true
			Spin = false
			Rotating = true
			Mode10()
		elseif C2 == "11" then
			Following = true
			Spin = false
			Rotating = true
			Mode11()
		elseif C2 == "12" then
			Following = true
			Spin = false
			Rotating = true
			Mode12()
		elseif C2 == "13" then
			Following = true
			Spin = false
			Rotating = true
			Mode13()
		elseif C2 == "14" then
			Following = true
			Spin = false
			Rotating = false
			Mode14()
		elseif C2 == "15" then
			Following = true
			Spin = false
			Rotating = true
			Mode15()
		elseif C2 == "16" then
			Following = true
			Spin = false
			Rotating = true
			Mode16()
		elseif C2 == "17" then
			Following = true
			Spin = false
			Rotating = true
			Mode17()
		end

	elseif C1 == ".offset" then
		Offset = tonumber(C2)
		
		Notify({
			Description = "Offset is now "..Offset..".";
			Title = "FE Hat Universe";
			Duration = 2;
		});
	elseif C1 == ".speed" then
		Speed = tonumber(C2)
		
		Notify({
			Description = "Speed is now "..Speed..".";
			Title = "FE Hat Universe";
			Duration = 2;
		});
	elseif C1 == ".height" then
		Height = tonumber(C2)
		
		Notify({
			Description = "Height is now "..Height..".";
			Title = "FE Hat Universe";
			Duration = 2;
		});
	elseif C1 == ".power" then
		Power = tonumber(C2)
		
		Notify({
			Description = "Power is now "..Power..".";
			Title = "FE Hat Universe";
			Duration = 2;
		});
	elseif C1 == ".orbit" then
		if C2 == "me" then
			Target = Char
		elseif C2 == "random" then
			local randomPlayer = game.Players:GetPlayers()[math.random(1, #game.Players:GetPlayers())]
			Target = randomPlayer.Character	
		elseif C2 == "nearest" then
			local minDistance = math.huge
			for _, player in pairs(game.Players:GetPlayers()) do
				if player.Character and player.Character ~= Char then
					local distance = (player.Character.HumanoidRootPart.Position - Char.HumanoidRootPart.Position).magnitude
					if distance < minDistance then
						minDistance = distance
						Target = player.Character
					end
				end
			end
		elseif C2 == "farthest" then
			local maxDistance = -math.huge
			for _, player in pairs(game.Players:GetPlayers()) do
				if player.Character and player.Character ~= Char then
					local distance = (player.Character.HumanoidRootPart.Position - Char.HumanoidRootPart.Position).magnitude
					if distance > maxDistance then
						maxDistance = distance
						Target = player.Character
					end
				end
			end
		else
			Target = findChar(C2)
		end
		
		Notify({
			Description = "Now orbiting "..Target.Name..".";
			Title = "FE Hat Universe";
			Duration = 2;
		});
	elseif C1 == ".blockhats" then
		if not Char:FindFirstChild("UpperTorso") then
			for i, v in pairs(Accessories) do
				if v.Handle:FindFirstChild("Mesh") then
					v.Handle:FindFirstChild("Mesh"):Remove()
				else
					v.Handle:FindFirstChild("SpecialMesh"):Remove()
				end
			end
			
			Notify({
				Description = "Hats are now blocks.";
				Title = "FE Hat Universe";
				Duration = 2;
			});
		else
			Notify({
				Description = "You need to be r6 to do this.";
				Title = "FE Hat Universe";
				Duration = 2;
			});
		end
	end
end)

Humanoid.Died:Connect(function()
	connect:Disconnect()
	AkaliNotif = 0
	Notify = 0
end)

for i = 1, 20 do
	Netless()
end
end)

